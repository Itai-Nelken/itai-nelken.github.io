manage.sh
=========

manage.sh is a script to help manage multiple local git repos. it can update all of them, push all of them, you can tell it which repos to ignore , and more will come.

to use this script, simply put it in a folder containing all your repos and make it executable if needed.
run './manage.sh --help' to see a list of all the commands.
